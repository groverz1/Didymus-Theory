<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MSU Research Terminal</title>
    <style>
        body { 
            background-color: #020202; 
            color: #00FF00; 
            font-family: "Courier New", Courier, monospace; 
            padding: 20px; 
            margin: 0;
            overflow-x: hidden;
            text-shadow: 0 0 5px rgba(0, 255, 0, 0.4);
        }
        /* Scanline Overlay Effect */
        body::before {
            content: " ";
            display: block;
            position: fixed;
            top: 0; left: 0; bottom: 0; right: 0;
            background: linear-gradient(rgba(18, 16, 16, 0) 50%, rgba(0, 0, 0, 0.3) 50%), linear-gradient(90deg, rgba(255, 0, 0, 0.03), rgba(0, 255, 0, 0.01), rgba(0, 0, 255, 0.03));
            z-index: 10;
            background-size: 100% 4px, 3px 100%;
            pointer-events: none;
        }
        #boot-screen { 
            position: fixed; 
            top: 0; left: 0; width: 100%; height: 100%; 
            background: black; 
            color: #00FF00; 
            display: flex; 
            align-items: center; 
            justify-content: center; 
            flex-direction: column; 
            z-index: 5;
            font-size: 20px;
        }
        #terminal-container {
            max-width: 900px;
            margin: 0 auto;
            display: none;
        }
        #terminal { 
            white-space: pre-wrap; 
            line-height: 1.5;
            margin-bottom: 20px;
        }
        .input-line {
            display: flex;
            align-items: center;
        }
        .prompt {
            color: #00FF00;
            font-weight: bold;
            margin-right: 10px;
            white-space: nowrap;
        }
        #input { 
            background: transparent; 
            color: #00FF00; 
            border: none; 
            font-family: "Courier New", Courier, monospace; 
            font-size: 16px;
            width: 100%; 
            outline: none;
            text-shadow: 0 0 5px rgba(0, 255, 0, 0.4);
        }
        /* Video Container Layout */
        #video-screen {
            display: none;
            max-width: 800px;
            margin: 40px auto;
            text-align: center;
            border: 3px double #00FF00;
            padding: 20px;
            background: rgba(0, 255, 0, 0.02);
            box-shadow: 0 0 15px rgba(0, 255, 0, 0.2);
        }
        .video-wrapper {
            position: relative;
            padding-bottom: 56.25%; /* 16:9 Aspect Ratio */
            height: 0;
            overflow: hidden;
            margin-top: 20px;
            border: 1px solid #00FF00;
        }
        .video-wrapper iframe, .video-wrapper video {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background: #000;
        }
        .system-alert {
            color: #FF3300;
            text-shadow: 0 0 5px rgba(255, 51, 0, 0.6);
        }
        .system-success {
            color: #00FFFF;
            text-shadow: 0 0 5px rgba(0, 255, 255, 0.6);
        }
    </style>
</head>
<body>

    <!-- Loading Screen -->
    <div id="boot-screen">
        <div>LOADING DIDYMUS CORE SYSTEM v4.8...</div>
        <div id="progress">⚡ [░░░░░░░░░░] 0%</div>
    </div>

    <!-- Active Main Terminal Frame -->
    <div id="terminal-container">
        <div id="terminal">===================================================================
MSU RESEARCH TERMINAL // MAIN SYSTEM OVERLOAD
TYPE 'help' FOR A LIST OF AVAILABLE COMMANDS.
===================================================================</div>
        <div class="input-line" id="input-row">
            <span class="prompt">SYS@DIDYMUS-CORE:~$</span>
            <input type="text" id="input" autofocus autocomplete="off" spellcheck="false">
        </div>
    </div>

    <!-- Target Hidden Video Page Frame -->
    <div id="video-screen">
        <h2 class="system-success">🔓 DECRYPTION COMPLETE: HAPPY BIRTHDAY, DAD!</h2>
        <p>The firewall has dropped. Your classified video transmission is ready below:</p>
        
        <div class="video-wrapper">
            <!-- 🔴 EDIT THE VIDEO SOURCE LINK BELOW HERE 🔴 -->
            <!-- If using YouTube, paste the /embed/ link here. Otherwise use a HTML <video> tag -->
            <iframe src="https://www.youtube.com/embed/dQw4w9WgXcQ" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
        </div>
        <br>
        <p style="font-size: 12px; color: #888;">System Status: Secure Connection Established.</p>
    </div>

    <!-- Audio Placeholders kept intact from your original layout -->
    <audio id="keypress-sound" src="data:audio/wav;base64,UklGRiQAAABXQVZFZm10IBAAAAABAAEAIlYAAESsAAACABAAZGF0YRAAAAAA"></audio>
    <audio id="boot-sound" src="data:audio/wav;base64,UklGRlIAAABXQVZFZm10IBAAAAABAAEAIlYAAESsAAACABAAZGF0YVQAAAAA"></audio>
    <audio id="success-sound" src="data:audio/wav;base64,UklGRiQAAABXQVZFZm10IBAAAAABAAEAIlYAAESsAAACABAAZGF0YW8AAAAA"></audio>
    <audio id="error-sound" src="data:audio/wav;base64,UklGRiQAAABXQVZFZm10IBAAAAABAAEAIlYAAESsAAACABAAZGF0YW8AAAAA"></audio>

    <script>
        const terminal = document.getElementById("terminal");
        const inputField = document.getElementById("input");
        const inputRow = document.getElementById("input-row");

        function playSound(id) {
            try { document.getElementById(id).play(); } catch(e) {}
        }

        // Artificial Progress Loader Bar
        let percent = 0;
        const progressInterval = setInterval(() => {
            percent += 20;
            let blocks = "█".repeat(percent / 10) + "░".repeat(10 - (percent / 10));
            document.getElementById("progress").innerText = `⚡ [${blocks}] ${percent}%`;
            
            if(percent >= 100) {
                clearInterval(progressInterval);
                setTimeout(() => {
                    playSound("boot-sound");
                    document.getElementById("boot-screen").style.display = "none";
                    document.getElementById("terminal-container").style.display = "block";
                    inputField.focus();
                }, 400);
            }
        }, 300);

        // Keep input box locked focused
        document.addEventListener("click", () => inputField.focus());

        inputField.addEventListener("keypress", function(event) {
            playSound("keypress-sound");
            if (event.key === "Enter") {
                let userInput = this.value.trim();
                this.value = "";
                if (userInput !== "") {
                    processCommand(userInput);
                }
            }
        });

        function writeLine(text, styleClass = "") {
            const span = document.createElement("span");
            if (styleClass) span.className = styleClass;
            span.innerText = "\n" + text;
            terminal.appendChild(span);
            window.scrollTo(0, document.body.scrollHeight);
        }

        function processCommand(cmd) {
            // Echo input back out
            writeLine(`SYS@DIDYMUS-CORE:~$ ${cmd}`);
            
            let normalizedCmd = cmd.toLowerCase();

            // 1. Check for the Secret Winning Password Match
            if (cmd === "HappyB1rthday") {
                playSound("success-sound");
                writeLine("\n[!] PASSWORD VALIDATED. ACCESS GRANTED.", "system-success");
                writeLine("INITIALIZING SOURCE TRANSFER STANDBY...", "system-success");
                
                setTimeout(() => {
                    document.getElementById("terminal-container").style.display = "none";
                    document.getElementById("video-screen").style.display = "block";
                }, 1500);
                return;
            }

            // 2. Command Route Menu
            switch (normalizedCmd) {
                case 'help':
                    writeLine("===============================================================");
                    writeLine("AVAILABLE COMMAND ARCHIVES:");
                    writeLine("  help               - Displays this manual resource menu");
                    writeLine("  Retrieve Text      - Loads advanced cosmic framework theories");
                    writeLine("  Binary Function    - Unpacks core localized identity values");
                    writeLine("  System Diagnostics - Tests system core performance nodes");
                    writeLine("  Matrix Mode        - Initiates secondary environmental layout cascades");
                    writeLine("  Self Destruct      - Decouples server grid frameworks completely");
                    writeLine("  clear              - Resets screen buffers");
                    writeLine("===============================================================");
                    break;

                case 'retrieve text':
                    writeLine("\n[RETRIEVING FILE: DIDYMUS_THEORY.DAT]...");
                    writeLine("----------------------------------------------------------------", "system-success");
                    writeLine("THE DIDYMUS THEORY OF THE COSMOS:");
                    writeLine("Scientific consensus holds that existence originated from a random singual inflation.");
                    writeLine("However, localized deep-space telemetry maps suggest a profound underlying pattern:");
                    writeLine("All functions of life, the universe, and everything can be directly traced");
                    writeLine("back to the exact engineering operations of a specialized Father.");
                    writeLine("\nThis dynamic entity teaches core principles of baseline Creativity.");
                    writeLine("The execution of this creativity is so potent, it inevitably expands past its");
                    writeLine("own boundaries, actively forging its own unique, self-sustaining universes.");
                    writeLine("Conclusion: You don't live in a universe. You live inside a masterpiece.");
                    writeLine("----------------------------------------------------------------", "system-success");
                    break;

                case 'binary function':
                    writeLine("\n[RUNNING CORE SYSTEM BINARY STREAM... DECODING KEY VALUES]");
                    writeLine("01001000 01100001 01110000 01110000 01111001 01000010");
                    writeLine("00110001 01110010 01110100 01101000 01100100 01100001 01111001");
                    writeLine("\nHINT: Copy/paste that exact binary sequence into an ASCII binary translator...");
                    writeLine("Or simply look closely at the upper/lowercase letters hidden inside.");
                    writeLine("Type the secret message exactly as your password command line choice to override core files.");
                    break;

                case 'system diagnostics':
                    writeLine("\n[!] CORE CHECK ACTIVE...");
                    writeLine("CPU: 100% OPERATIONAL");
                    writeLine("MEMORY ARRAY: 64KB OK");
                    writeLine("EMOTIONAL EMISSION LEVEL: MAXIMAL CAPACITY");
                    writeLine("FATHERLY AWESOMENESS METERS: BEYOND COMPUTATIONAL CAPACITY.");
                    break;

                case 'matrix mode':
                    writeLine("\n[CONNECTING TO INTERFACE CASCADE...]");
                    let counter = 0;
                    const interval = setInterval(() => {
                        writeLine("01101101 01100001 01110100 01110010 01101001 01111001 ".repeat(2));
                        counter++;
                        if(counter > 12) clearInterval(interval);
                    }, 80);
                    break;

                case 'self destruct':
                    playSound("error-sound");
                    writeLine("\n[!!!] CRITICAL ERROR: SELF DESTRUCT INITIATED.", "system-alert");
                    let count = 5;
                    const countdown = setInterval(() => {
                        if(count > 0) {
                            writeLine(`T-MINUS ${count} SECONDS...`, "system-alert");
                            count--;
                        } else {
                            clearInterval(countdown);
                            writeLine("...Just kidding! Happy Birthday! (Now go crack that password!)", "system-success");
                        }
                    }, 1000);
                    break;

                case 'clear':
                    terminal.innerText = "===================================================================\nMSU RESEARCH TERMINAL // MAIN SYSTEM OVERLOAD\n===================================================================";
                    break;

                default:
                    playSound("error-sound");
                    writeLine(`ERR: Command string '${cmd}' unrecognized. Type 'help' to review directory.`, "system-alert");
            }
        }
    </script>
</body>
</html>
